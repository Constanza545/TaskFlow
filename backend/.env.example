# Puerto del servidor
PORT=4000

# Secreto para firmar los JWT (usa un valor largo y aleatorio en producción)
JWT_SECRET=cambia-este-valor-por-uno-seguro

# Tiempo de expiración del token
JWT_EXPIRES_IN=8h

# Ruta del archivo SQLite
DB_PATH=./data/taskflow.sqlite

# Origen permitido para CORS (frontend)
CORS_ORIGIN=http://localhost:5173

# Intervalo (ms) para refrescar precios de criptomonedas vía CoinGecko
CRYPTO_POLL_INTERVAL_MS=15000

# Cuentas demo opcionales (para instancias de demostración pública).
# Si ambas variables están definidas, al arrancar se crean tres cuentas con la
# misma contraseña: este email como admin, más miembro@<dominio> e
# invitado@<dominio>, para poder probar los tres roles.
# DEMO_USER_EMAIL=demo@ejemplo.com
# DEMO_USER_PASSWORD=una-clave-segura
